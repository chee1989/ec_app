<% flash.each do |key, value| %>
  <% if key.present? %>
    <div class='alert alert-<%= bootstrap_alert(key) %>'>
      <strong>
        <%= value %>
      </strong>
    </div>
  <% end %>
<% end %>
