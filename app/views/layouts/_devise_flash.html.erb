<% flash.each do |key, value| %>
  <% next if value == 'true' %>
  <% if key.present? %>
    <div class='alert alert-<%= bootstrap_alert(key) %>'>
      <strong>
        <%= value %>
      </strong>
    </div>
  <% end %>
<% end %>
